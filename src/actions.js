import axios from "./axios";

export async function submitUser(path, fields) {
    // const { data } = await axios.post(path, fields);
    // return {
    //     type: "SUBMIT_USER_INFO",
    //     success: data.success,
    //     user: data.user,
    // };
}

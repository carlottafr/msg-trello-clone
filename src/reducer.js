export default function reducer(state = {}, action) {
    // if (action.type == "SUBMIT_USER_INFO") {
    //     state = {
    //         ...state,
    //         success: action.success,
    //         user: action.user,
    //     };
    // }
    // return state;
}
